<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alatheer.anwaralfyaha">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        tools:replace="android:supportsRtl"
        android:theme="@style/AppTheme">
        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name=".Activities.Select"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.ghyaab"
            android:label="@string/activity_ghyaab" />
        <activity
            android:name=".Activities.Splach"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.News"
            android:label="@string/news" />
        <activity
            android:name=".Activities.Elmstwa"
            android:label="@string/mustwa" />
        <activity
            android:name=".Activities.Sharf"
            android:label="@string/sharf" />
        <activity
            android:name=".Activities.Table"
            android:label="@string/gdwal" />
        <activity
            android:name=".Activities.Rsoom"
            android:label="@string/activity_rsoom" />
        <activity
            android:name=".Activities.Works"
            android:label="@string/activity_works" />
        <activity android:name=".Activities.Detail" />
        <activity
            android:name=".Activities.Home"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.Login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.School"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Activities.Anshta"
            android:label="@string/activity_anshta" />
        <activity android:name=".Activities.Tafasel" />
        <activity
            android:name=".Activities.Arcmenu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.WebViiew"
            android:label="@string/title_activity_web_viiew" />
        <activity android:name=".Activities.Notify" />
        <activity
            android:name=".Activities.MyIntro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.PaySchool"
            android:label="@string/activity_payschool" />
        <activity
            android:name=".Activities.nafic"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.TargetActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.Notification" />
        <activity android:name=".Activities.Copy"></activity>
    </application>

</manifest>